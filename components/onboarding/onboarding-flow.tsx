"use client";

import { useRef, useState } from "react";
import type { AddressTruthReport, AnalysisRequest, AnchorCategory } from "@/lib/domain/analysis";
import type { UserPreset } from "@/lib/domain/user-profile";
import { AnalysisReport } from "@/components/report/analysis-report";
import { buildAnalysisRequest, createAnchor, OnboardingDraftError, type AnchorDraft, type OnboardingErrors, validatePropertyAddress } from "@/lib/onboarding/draft";

type Step = "property" | "routine" | "review" | "analysing" | "report";
const categories: AnchorCategory[] = ["work", "education", "health", "social", "exercise", "other"];
const presets: UserPreset[] = ["student", "professional", "family", "custom"];

function FieldError({ message }: { message?: string }) { return message ? <p role="alert" className="mt-1 text-sm text-danger">{message}</p> : null; }

export function OnboardingFlow() {
  const [step, setStep] = useState<Step>("property");
  const [propertyAddress, setPropertyAddress] = useState("");
  const [propertyError, setPropertyError] = useState<string>();
  const [preset, setPreset] = useState<UserPreset>("custom");
  const [timeLens, setTimeLens] = useState(true);
  const [anchors, setAnchors] = useState<AnchorDraft[]>([createAnchor("anchor-1")]);
  const [errors, setErrors] = useState<OnboardingErrors>({});
  const [request, setRequest] = useState<AnalysisRequest>();
  const [report, setReport] = useState<AddressTruthReport>();
  const [analysisError, setAnalysisError] = useState<string>();
  const nextId = useRef(2);

  function updateAnchor(index: number, field: keyof AnchorDraft, value: string) {
    setAnchors((current) => current.map((anchor, position) => position === index ? { ...anchor, [field]: value } : anchor));
    setErrors((current) => ({ ...current, [`anchors.${index}.${field}`]: "" }));
  }
  function addAnchor() { setAnchors((current) => current.length < 4 ? [...current, createAnchor(`anchor-${nextId.current++}`)] : current); }
  function removeAnchor(index: number) { setAnchors((current) => current.length > 1 ? current.filter((_, position) => position !== index) : current); }
  function submitProperty(event: React.FormEvent) { event.preventDefault(); const error = validatePropertyAddress(propertyAddress); setPropertyError(error); if (!error) setStep("routine"); }
  function submitRoutine(event: React.FormEvent) { event.preventDefault(); try { setRequest(buildAnalysisRequest({ propertyAddress, preset, anchors, timeLens })); setErrors({}); setStep("review"); } catch (error) { if (error instanceof OnboardingDraftError) setErrors(error.fieldErrors); else throw error; } }
  async function analyse() {
    if (!request) return; setAnalysisError(undefined); setStep("analysing");
    try { const response = await fetch("/api/analyse", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(request) }); const payload: unknown = await response.json(); if (!response.ok || !payload || typeof payload !== "object" || !("property" in payload) || !("routes" in payload) || !("modules" in payload)) throw new Error(payload && typeof payload === "object" && "error" in payload && typeof payload.error === "string" ? payload.error : "We could not complete this address analysis. Please try again."); setReport(payload as AddressTruthReport); setStep("report"); } catch (error) { setAnalysisError(error instanceof Error ? error.message : "We could not complete this address analysis. Please try again."); setStep("review"); }
  }
  if (step === "report" && report) return <AnalysisReport report={report} onEdit={() => setStep("review")} />;
  if (step === "analysing") return <section aria-live="polite" className="border border-border bg-paper p-6"><p className="font-mono text-xs uppercase tracking-[.16em] text-moss">Analysing your routine</p><h2 className="mt-2 text-2xl font-semibold">Checking the journeys that shape your week</h2><p className="mt-3 text-sm text-muted-ink">Requesting route data and calculating your weekly burden.</p></section>;
  if (step === "review" && request) return <section className="border border-border bg-paper p-6 sm:p-8"><p className="font-mono text-xs uppercase tracking-[.16em] text-moss">Details ready</p><h2 className="mt-2 text-2xl font-semibold">Your weekly routine</h2><p className="mt-2 text-sm text-muted-ink">{request.property.address} · {request.userProfile?.preset}</p><ul className="mt-6 divide-y divide-border border-y border-border">{request.anchors.map((anchor) => <li key={anchor.id} className="py-3 text-sm"><strong>{anchor.name}</strong> · {anchor.visitsPerWeek} visits/week · {anchor.maxTravelMinutes} min limit</li>)}</ul>{analysisError && <p role="alert" className="mt-4 text-sm text-danger">{analysisError}</p>}<div className="mt-6 grid gap-3 sm:grid-cols-2"><button className="button-secondary" type="button" onClick={() => setStep("routine")}>Edit routine</button><button className="button-primary" type="button" onClick={analyse}>Analyse this address <span>→</span></button></div></section>;
  if (step === "routine") return <section className="border border-border bg-paper p-6 sm:p-8"><p className="font-mono text-xs uppercase tracking-[.16em] text-muted-ink">Step 02</p><h2 className="mt-2 text-2xl font-semibold">Add the places that shape your week</h2><p className="mt-2 text-sm text-muted-ink">{propertyAddress.trim()} · add up to four destinations.</p><form className="mt-6 space-y-6" noValidate onSubmit={submitRoutine}><div><label className="block text-sm font-medium" htmlFor="preset">Your routine</label><select id="preset" className="form-control mt-2" value={preset} onChange={(e) => setPreset(e.target.value as UserPreset)}>{presets.map((item) => <option key={item} value={item}>{item[0].toUpperCase() + item.slice(1)}</option>)}</select></div>{anchors.map((anchor, index) => <fieldset key={anchor.id} className="border border-border p-4"><legend className="px-2 text-sm font-semibold">Regular place {index + 1}</legend><div className="grid gap-4 sm:grid-cols-2"><div><label htmlFor={`name-${anchor.id}`}>Place name</label><input id={`name-${anchor.id}`} className="form-control mt-1" value={anchor.name} onChange={(e) => updateAnchor(index, "name", e.target.value)} /><FieldError message={errors[`anchors.${index}.name`]} /></div><div><label htmlFor={`address-${anchor.id}`}>Destination address</label><input id={`address-${anchor.id}`} className="form-control mt-1" value={anchor.address} onChange={(e) => updateAnchor(index, "address", e.target.value)} /><FieldError message={errors[`anchors.${index}.address`]} /></div><div><label htmlFor={`category-${anchor.id}`}>Category</label><select id={`category-${anchor.id}`} className="form-control mt-1" value={anchor.category} onChange={(e) => updateAnchor(index, "category", e.target.value)}>{categories.map((item) => <option key={item} value={item}>{item}</option>)}</select></div><div className="grid grid-cols-2 gap-3"><div><label htmlFor={`visits-${anchor.id}`}>Visits/week</label><input id={`visits-${anchor.id}`} className="form-control mt-1" type="number" min="1" value={anchor.visitsPerWeek} onChange={(e) => updateAnchor(index, "visitsPerWeek", e.target.value)} /><FieldError message={errors[`anchors.${index}.visitsPerWeek`]} /></div><div><label htmlFor={`limit-${anchor.id}`}>Max minutes</label><input id={`limit-${anchor.id}`} className="form-control mt-1" type="number" min="1" value={anchor.maxTravelMinutes} onChange={(e) => updateAnchor(index, "maxTravelMinutes", e.target.value)} /><FieldError message={errors[`anchors.${index}.maxTravelMinutes`]} /></div></div></div>{anchors.length > 1 && <button type="button" onClick={() => removeAnchor(index)} className="mt-4 text-sm text-danger">Remove this place</button>}</fieldset>)}{errors.anchors && <FieldError message={errors.anchors} />}<button type="button" disabled={anchors.length === 4} onClick={addAnchor} className="button-secondary w-full disabled:cursor-not-allowed disabled:opacity-50">Add another regular place ({anchors.length}/4)</button><label className="flex gap-3 text-sm"><input type="checkbox" checked={timeLens} onChange={(e) => setTimeLens(e.target.checked)} />Compare morning and evening for your most frequent destinations</label><div className="grid gap-3 sm:grid-cols-2"><button type="button" className="button-secondary" onClick={() => setStep("property")}>Back</button><button type="submit" className="button-primary">Review routine <span>→</span></button></div></form></section>;
  return <section className="border border-border bg-paper p-6 sm:p-8"><p className="font-mono text-xs uppercase tracking-[.16em] text-muted-ink">Step 01</p><h2 className="mt-2 text-2xl font-semibold">Start with the address</h2><form className="mt-6" noValidate onSubmit={submitProperty}><label htmlFor="property-address">Potential property</label><input id="property-address" className="form-control mt-2" value={propertyAddress} onChange={(e) => setPropertyAddress(e.target.value)} autoComplete="street-address" /><FieldError message={propertyError} /><button className="button-primary mt-5 w-full" type="submit">Continue to your routine <span>→</span></button></form></section>;
}
